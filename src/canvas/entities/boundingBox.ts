// TODO: Bounding box operations
// - createBoundingBox(entity)
// - expandBoundingBox(box, padding)
// - getBoundingBoxCorners(box)
// - etc.

export interface BoundingBox {
  x: number;
  y: number;
  width: number;
  height: number;
}
